# コピーして .env にリネームし、値だけ埋める。.env は Git に含めない（.gitignore 済み）。
# CI では GitHub Actions の Secrets を使う。

RESEND_API_KEY=
NOTIFY_EMAIL_TO=
NOTIFY_EMAIL_FROM=onboarding@resend.dev

# SMTP を使う場合（未使用ならコメントのまま）
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=

# NOTIFY_EMAIL_DISABLED=true
